const auid = require('./auid')
const facade = require('./facade')
const HTML = require('./HTML')
const pipe = require('./pipe')
const sistre = require('./sistre')
const T = require('./T')

module.exports = { auid, facade, HTML, pipe, sistre, T }
